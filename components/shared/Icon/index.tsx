// interface IIcon {
//   name: "BrandReact";
// }

// const TablerIcon = ({ name }: { name: string }) => {
//   `<${prop}/>`;
// };

// const Icon = ({ name }: IIcon) => {
//   return <Tabler name={name} />;
// };

// export default Icon;
